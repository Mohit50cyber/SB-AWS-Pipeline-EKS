apiVersion: v1
kind: Service
metadata:
  name: mohit-kubernetes
spec:
  selector:
    app: mohit-kubernetes
  ports:
    - protocol: "TCP"
      port: 6000
      targetPort: 6000
  type: NodePort



